# คู่มือการติดตั้งและใช้งานระบบ Shift Work Management

## 🎯 ภาพรวมระบบ

ระบบจัดการเวรปฏิบัติงานและบริหารค่าใช้จ่ายสำหรับนิติบุคคลอาคารชุดและหมู่บ้านจัดสรร พัฒนาด้วย:
- **Backend**: Node.js + Express + Prisma + PostgreSQL
- **Frontend**: React + Vite + Ant Design + TypeScript

## 📋 ฟีเจอร์ที่สำคัญ

### ✅ 1. Project Management & Cost Sharing
- สร้าง/แก้ไข/ลบโครงการ
- ตั้งค่า Cost Sharing (แชร์ค่าใช้จ่ายข้ามโครงการ)
- กำหนดสัดส่วน % การแชร์ไปยังโครงการอื่น

### ✅ 2. Staff Management  
- จัดการข้อมูลพนักงาน (ชื่อ, ตำแหน่ง, ค่าแรง, เบอร์โทร)
- รองรับประเภทพนักงาน: Regular (ประจำ) และ Spare (สแปร์)
- Toggle Active/Inactive (ไม่ลบข้อมูลเพื่อเก็บประวัติ)
- กำหนดกะทำงานตั้งต้น

### ✅ 3. Roster Management (ตารางเวร)
- แสดงตารางเวรแบบ Grid (พนักงาน x วันที่)
- รองรับรหัสกะ: 1, 2, 3, ดึก, OFF, ข (ขาด), ป (ลาป่วย), ก (ลากิจ), พ (พักร้อน)
- Color Coding สำหรับแต่ละประเภทกะ
- คลิกเพื่อสลับกะได้ทันที
- แสดงสถิติรายวัน (จำนวนคนเข้างาน/OFF/ขาด/ลา)

### ✅ 4. Reports & Export
- รายงานหักเงินประจำเดือน (Monthly Deduction Report)
- รายงาน Cost Sharing (แสดงการแชร์ค่าใช้จ่ายข้ามโครงการ)
- Export เป็น CSV และ Excel (.xlsx)
- แสดงสรุปสถิติ (จำนวนวันทำงาน, ขาด, ลา, ยอดเงิน)

## 🚀 การติดตั้ง

### ข้อกำหนดเบื้องต้น
- Node.js >= 18
- PostgreSQL >= 14
- pnpm หรือ npm

### 1. ติดตั้ง Backend

```bash
cd backend

# ติดตั้ง dependencies
npm install

# ตั้งค่า environment variables
# สร้างไฟล์ .env และใส่:
DATABASE_URL="postgresql://user:password@localhost:5432/shift_work"
JWT_SECRET="your-secret-key-here"
PORT=3001

# สร้าง database schema
npx prisma migrate dev

# Seed ข้อมูลตัวอย่าง (optional)
npx prisma db seed

# รันเซิร์ฟเวอร์
npm run dev
```

Backend จะรันที่ `http://localhost:3001`

### 2. ติดตั้ง Frontend

```bash
cd frontend

# ติดตั้ง dependencies
npm install

# รัน development server
npm run dev
```

Frontend จะรันที่ `http://localhost:5173`

## 📖 การใช้งาน

### 1. การจัดการโครงการ

#### เพิ่มโครงการใหม่
1. ไปที่เมนู **โครงการ**
2. คลิก **เพิ่มโครงการ**
3. กรอกข้อมูล: ชื่อโครงการ, สถานที่, คำอธิบาย, สีธีม
4. คลิก **บันทึก**

#### ตั้งค่า Cost Sharing
1. ในรายการโครงการ คลิกปุ่ม **Cost Sharing** ที่โครงการต้นทาง
2. คลิก **เพิ่มการแชร์ค่าใช้จ่าย**
3. เลือก **โครงการปลายทาง** และกรอก **สัดส่วน %**
4. สามารถเพิ่มหลายโครงการได้
5. คลิก **บันทึก**

**ตัวอย่าง**: โครงการ A แชร์ค่าใช้จ่ายให้ โครงการ B (30%) และ โครงการ C (20%)

### 2. การจัดการพนักงาน

#### เพิ่มพนักงาน
1. ไปที่เมนู **พนักงาน**
2. เลือก**โครงการ** จาก dropdown
3. คลิก **เพิ่มพนักงาน**
4. กรอกข้อมูล:
   - ชื่อพนักงาน
   - ตำแหน่ง
   - เบอร์โทร (optional)
   - ค่าแรงต่อวัน (บาท)
   - ประเภทพนักงาน (ประจำ/สแปร์)
   - กะทำงานตั้งต้น
5. คลิก **บันทึก**

#### เปิด/ปิดสถานะพนักงาน
- ใช้ **Switch** ในคอลัมน์สถานะ
- พนักงานที่ปิด (Inactive) จะไม่แสดงในตารางเวรใหม่
- แต่ข้อมูลจะยังเก็บไว้สำหรับรายงานย้อนหลัง

### 3. การจัดตารางเวร

#### สร้าง/แก้ไขตารางเวร
1. ไปที่เมนู **ตารางเวร**
2. เลือก **โครงการ** และ **เดือน/ปี**
3. ระบบจะแสดงตารางแบบ Grid (พนักงาน x วันที่ 1-31)
4. **คลิกที่ช่อง** เพื่อสลับกะ (1 → 2 → 3 → OFF → 1)
5. การเปลี่ยนแปลงจะบันทึกอัตโนมัติ

#### สัญลักษณ์กะ
- **กะ 1, 2, 3, ดึก**: กะทำงาน (สีเขียว, น้ำเงิน, ม่วง, ชมพู)
- **OFF**: วันหยุด (สีเทา)
- **ข**: ขาดงาน (สีแดง)
- **ป**: ลาป่วย (สีส้ม)
- **ก**: ลากิจ (สีส้มอ่อน)
- **พ**: พักร้อน (สีเขียวมิ้นต์)

### 4. ดูรายงานและส่งออก

#### รายงานหักเงินประจำเดือน
1. ไปที่เมนู **รายงาน** → แท็บ **รายงานหักเงิน**
2. เลือก **โครงการ** และ **เดือน/ปี**
3. ระบบจะแสดง:
   - จำนวนวันทำงาน
   - จำนวนวันขาด/ลา
   - เงินเดือนคาดหวัง
   - ยอดหักเงิน
   - เงินเดือนสุทธิ
4. คลิก **ดาวน์โหลด CSV** หรือ **ดาวน์โหลด Excel**

#### รายงาน Cost Sharing
1. ไปที่เมนู **รายงาน** → แท็บ **รายงาน Cost Sharing**
2. เลือก **เดือน/ปี**
3. ระบบจะแสดง:
   - ต้นทุนเดิมของแต่ละโครงการ
   - ค่าใช้จ่ายที่แชร์ออกไป (Shared Out)
   - ค่าใช้จ่ายที่รับเข้ามา (Shared In)
   - ต้นทุนสุทธิ (Net Cost)

## 🔧 API Endpoints

### Projects
```
GET    /api/projects              - ดึงรายการโครงการทั้งหมด
GET    /api/projects/:id          - ดึงโครงการตาม ID
POST   /api/projects              - สร้างโครงการใหม่
PUT    /api/projects/:id          - แก้ไขโครงการ
PUT    /api/projects/:id/cost-sharing - อัพเดต Cost Sharing
DELETE /api/projects/:id          - ลบโครงการ
```

### Staff
```
GET    /api/staff?projectId=xxx   - ดึงรายการพนักงานของโครงการ
GET    /api/staff/:id             - ดึงพนักงานตาม ID
POST   /api/staff                 - สร้างพนักงานใหม่
PUT    /api/staff/:id             - แก้ไขพนักงาน
PATCH  /api/staff/:id/toggle-status - เปิด/ปิดสถานะพนักงาน
DELETE /api/staff/:id             - ลบพนักงาน
```

### Rosters
```
GET    /api/rosters?projectId=xxx&year=2567&month=1  - ดึงตารางเวร
GET    /api/rosters/stats?rosterId=xxx&day=15        - ดึงสถิติรายวัน
POST   /api/rosters/entry         - อัพเดตกะของพนักงาน
POST   /api/rosters/batch         - อัพเดตหลายกะพร้อมกัน
DELETE /api/rosters/entry/:id     - ลบรายการกะ
```

### Reports
```
GET /api/reports/deduction?projectId=xxx&year=2567&month=1  - รายงานหักเงิน
GET /api/reports/cost-sharing?year=2567&month=1             - รายงาน Cost Sharing
GET /api/reports/financial-overview?year=2567&month=1       - ภาพรวมการเงิน
GET /api/reports/export?projectId=xxx&year=2567&month=1     - Export CSV
```

## 📝 Database Schema

### หลักการออกแบบ
- **Projects**: เก็บข้อมูลโครงการและ theme color
- **CostSharing**: เก็บความสัมพันธ์การแชร์ค่าใช้จ่ายระหว่างโครงการ
- **Staff**: เก็บข้อมูลพนักงาน มีฟิลด์ `isActive` สำหรับเปิด/ปิดโดยไม่ลบข้อมูล
- **Roster**: ตารางเวรรายเดือนของแต่ละโครงการ
- **RosterEntry**: รายการกะของพนักงานแต่ละวัน
- **MonthlyAttendance**: สรุปการทำงานประจำเดือน (ยังไม่ได้ใช้งานเต็มรูปแบบ)

## 🎨 UI/UX Highlights

### Color Coding
- ใช้สีแยกประเภทกะให้เห็นชัดเจน
- พนักงาน Spare มี Tag สีส้ม
- พนักงาน Inactive แสดงเป็นสีเทาจาง

### Responsive Design
- ตารางเวรสามารถ scroll ซ้าย-ขวาได้
- รองรับหน้าจอขนาดต่างๆ

### Real-time Feedback
- แสดง Loading state ขณะโหลดข้อมูล
- แสดง Success/Error message เมื่อทำงานสำเร็จหรือล้มเหลว

## 🐛 การแก้ปัญหา

### Backend ไม่สามารถเชื่อมต่อ Database
```bash
# ตรวจสอบ PostgreSQL service
# Windows
services.msc → หา PostgreSQL

# ตรวจสอบ DATABASE_URL ใน .env
DATABASE_URL="postgresql://user:password@localhost:5432/dbname"

# รัน migration ใหม่
npx prisma migrate reset
npx prisma migrate dev
```

### Frontend ไม่สามารถเรียก API
1. ตรวจสอบว่า Backend รันอยู่ที่ port 3001
2. ตรวจสอบ CORS settings ใน backend
3. ตรวจสอบ API_URL ในไฟล์ Frontend (`http://localhost:3001/api`)

### ตารางเวรไม่แสดง
1. ตรวจสอบว่าเลือกโครงการแล้ว
2. ตรวจสอบว่ามีพนักงาน Active ในโครงการ
3. เช็ค Console ใน Browser DevTools

## 🔐 Security Notes

- ใช้ JWT สำหรับ Authentication (ควรเพิ่ม Login Page จริง)
- Password ควร hash ด้วย bcrypt
- ควรเพิ่ม Rate Limiting สำหรับ Production
- ตรวจสอบ Permissions ตาม Role (SUPER_ADMIN, SITE_MANAGER)

## 🚀 Production Deployment

### Backend
```bash
npm run build
npm start
```

### Frontend
```bash
npm run build
# deploy dist/ folder ไปยัง CDN หรือ Static hosting
```

## 📞 Support

หากมีปัญหาหรือข้อสงสัย สามารถติดต่อได้ที่ Developer หรือดูเอกสารเพิ่มเติมใน:
- Backend: `backend/README.md`
- Frontend: `frontend/README.md`
- Database Schema: `backend/prisma/schema.prisma`

---

**เวอร์ชัน**: 1.0.0  
**สร้างเมื่อ**: มกราคม 2026  
**License**: Private
