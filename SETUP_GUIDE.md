# 🚀 คู่มือการติดตั้งและเริ่มต้นใช้งาน

## ขั้นตอนการติดตั้งอย่างละเอียด

### 1. ติดตั้ง Prerequisites

#### Windows

**ติดตั้ง Node.js:**
1. ดาวน์โหลด Node.js LTS จาก [https://nodejs.org](https://nodejs.org)
2. ติดตั้งตามขั้นตอน (เลือก "Automatically install the necessary tools")
3. ตรวจสอบการติดตั้ง:
```powershell
node --version   # ควรได้ v18 ขึ้นไป
npm --version
```

**ติดตั้ง PostgreSQL:**
1. ดาวน์โหลด PostgreSQL จาก [https://www.postgresql.org/download/windows/](https://www.postgresql.org/download/windows/)
2. ติดตั้งและจดจำ Password ของ postgres user
3. เปิด pgAdmin4 (ติดตั้งมาด้วย)
4. สร้าง Database ใหม่ชื่อ `shift_work_db`

หรือใช้ Command Line:
```powershell
# เปิด Command Prompt as Administrator
psql -U postgres
# ใส่ password ที่ตั้งไว้
CREATE DATABASE shift_work_db;
\q
```

### 2. Setup โปรเจค

**Clone หรือ Navigate ไปยังโปรเจค:**
```powershell
cd "C:\Users\tanongsakn\Desktop\Project Dev\Shift Work SENX Juristic"
```

**ติดตั้ง Dependencies:**
```powershell
npm install
```

### 3. ตั้งค่า Environment Variables

**สร้างไฟล์ .env:**
```powershell
# คัดลอกจาก .env.example
copy .env.example .env

# แก้ไขด้วย notepad
notepad .env
```

**แก้ไข .env ให้ตรงกับการตั้งค่า PostgreSQL ของคุณ:**

```env
# ตัวอย่าง: ถ้า PostgreSQL ติดตั้งบน localhost, port 5432
# Username: postgres, Password: yourpassword, Database: shift_work_db
DATABASE_URL="postgresql://postgres:yourpassword@localhost:5432/shift_work_db?schema=public"

# สร้าง JWT Secret ใหม่ (ควรใช้ random string ยาวๆ)
JWT_SECRET="abc123xyz789-change-this-to-random-string"

# URL ของแอพ
NEXT_PUBLIC_APP_URL="http://localhost:3000"
```

### 4. สร้าง Database Schema

```powershell
# Push schema ไปยัง database
npm run db:push
```

คำสั่งนี้จะสร้างตารางทั้งหมดใน PostgreSQL ตาม schema ที่กำหนด

### 5. Seed ข้อมูลตัวอย่าง

```powershell
npm run db:seed
```

คำสั่งนี้จะสร้าง:
- ✅ Admin User (admin@senx.com / admin123)
- ✅ โครงการตัวอย่าง 3 โครงการ
- ✅ Cost Sharing ระหว่างโครงการ
- ✅ พนักงานตัวอย่าง 4 คน

### 6. รันเซิร์ฟเวอร์

```powershell
npm run dev
```

เปิดเบราว์เซอร์ไปที่: **http://localhost:3000**

---

## 🧪 ทดสอบระบบ

### เข้าสู่ระบบ
- URL: http://localhost:3000/login
- Email: `admin@senx.com`
- Password: `admin123`

### ทดสอบฟีเจอร์

**1. Dashboard:**
- ตรวจสอบว่าแสดงโครงการทั้ง 3 โครงการ
- ลองคลิก Quick Access ไปยังตารางเวรของเดือนต่างๆ

**2. จัดการโครงการ:**
- ทดสอบเพิ่มโครงการใหม่
- ทดสอบตั้งค่า Cost Sharing (เลือกโครงการปลายทางและสัดส่วน %)
- ทดสอบแก้ไขและลบโครงการ

**3. จัดการพนักงาน:**
- ทดสอบเพิ่มพนักงานใหม่ (ทั้งประจำและสแปร์)
- ทดสอบเปลี่ยนสถานะ Active/Inactive
- กรองพนักงานตามโครงการ

**4. ตารางเวร:**
- เลือกโครงการและเดือน
- ทดสอบเปลี่ยนกะของพนักงาน
- สังเกต Color Coding และ Badge สำหรับพนักงานสแปร์
- ทดสอบบันทึกการเปลี่ยนแปลง

---

## 🛠 Commands ที่สำคัญ

| Command | คำอธิบาย |
|---------|----------|
| `npm run dev` | รันเซิร์ฟเวอร์ Development |
| `npm run build` | Build สำหรับ Production |
| `npm run start` | รันเซิร์ฟเวอร์ Production |
| `npm run db:push` | Push Schema ไปยัง Database |
| `npm run db:studio` | เปิด Prisma Studio (GUI Database) |
| `npm run db:seed` | Seed ข้อมูลตัวอย่าง |
| `npm run db:generate` | Generate Prisma Client |

---

## 📊 ดู Database ด้วย Prisma Studio

```powershell
npm run db:studio
```

เปิดเบราว์เซอร์ไปที่: **http://localhost:5555**

Prisma Studio ช่วยให้คุณ:
- ดูข้อมูลในตารางทั้งหมด
- แก้ไขข้อมูลโดยตรง (GUI)
- ลบข้อมูล
- เพิ่มข้อมูลทดสอบ

---

## 🔐 การสร้าง Admin User เพิ่มเติม

ถ้าต้องการสร้าง Admin เพิ่ม ให้แก้ไข `prisma/seed.ts`:

```typescript
const newAdmin = await prisma.user.create({
  data: {
    email: 'manager@senx.com',
    password: await bcrypt.hash('password123', 10),
    name: 'Site Manager',
    role: 'SITE_MANAGER',
  },
});
```

จากนั้นรัน:
```powershell
npm run db:seed
```

---

## 📝 การ Backup และ Restore Database

### Backup
```powershell
pg_dump -U postgres -d shift_work_db -f backup.sql
```

### Restore
```powershell
psql -U postgres -d shift_work_db -f backup.sql
```

---

## 🚨 แก้ปัญหาที่พบบ่อย

### ปัญหา 1: "Error: Can't find Prisma Client"
**แก้ไข:**
```powershell
npm run db:generate
```

### ปัญหา 2: "Database connection failed"
**แก้ไข:**
1. ตรวจสอบว่า PostgreSQL กำลังรันอยู่
2. ตรวจสอบ DATABASE_URL ในไฟล์ `.env`
3. ลองเชื่อมต่อด้วย pgAdmin4 เพื่อยืนยัน

### ปัญหา 3: "Port 3000 already in use"
**แก้ไข:**
```powershell
# หยุด process ที่ใช้ port 3000
netstat -ano | findstr :3000
taskkill /PID <PID> /F

# หรือเปลี่ยน port ใน package.json
"dev": "next dev -p 3001"
```

### ปัญหา 4: Login ไม่ผ่าน "Unauthorized"
**แก้ไข:**
1. ตรวจสอบว่าได้รัน `npm run db:seed`
2. ตรวจสอบ JWT_SECRET ในไฟล์ `.env`
3. ลบ cookies ในเบราว์เซอร์

---

## 🎯 Next Steps

หลังจากติดตั้งเสร็จแล้ว:

1. **ปรับแต่ง UI:** แก้สี, โลโก้, ชื่อบริษัท
2. **เพิ่มข้อมูลจริง:** ลบข้อมูลตัวอย่างและเพิ่มข้อมูลจริง
3. **ตั้งค่า Email Notifications** (ถ้าต้องการ)
4. **Deploy ไปยัง Production** (Vercel, Railway, หรือ VPS)

---

ถ้ามีคำถามหรือปัญหาเพิ่มเติม กรุณาติดต่อทีมพัฒนา 🚀
